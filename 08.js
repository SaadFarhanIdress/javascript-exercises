// The below function takes two numbers 
// as arguments, adds them and print the
// result to the console.

function add(a, b) {
  return console.log(a + b);
}

add(5, 3);

// The below function takes two numbers 
// as arguments, subtracts them and print 
// the result to the console.

function subtract(a, b) {
  return console.log(a - b);
}

subtract(10 - 2);